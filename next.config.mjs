/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  // === STATIC EXPORT MODE ===
  output: 'export',
};

// Export ESM-style
export default nextConfig;
